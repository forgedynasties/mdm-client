<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:sharedUserId="android.uid.system"
    package="com.aioapp.mdm" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.REBOOT" />
    <uses-permission android:name="android.permission.MANAGE_DEVICE_ADMINS" />
    <uses-permission android:name="android.permission.MANAGE_PROFILE_AND_DEVICE_OWNERS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.INJECT_EVENTS" />

    <!-- Only platform-signed components (SystemUI) may trigger the offline kiosk-exit prompt. -->
    <permission
        android:name="com.aioapp.mdm.permission.KIOSK_EXIT"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.aioapp.mdm.permission.KIOSK_EXIT" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true">

        <receiver
            android:name=".MdmAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".MdmService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Offline kiosk-exit prompt. Whitelisted for lock-task alongside the kiosk app so it
             can surface over it; shown only via KioskExitReceiver on the nav-bar gesture. -->
        <activity
            android:name=".UnlockActivity"
            android:label="Kiosk"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog.NoActionBar" />

        <!-- SystemUI (long-press Back in kiosk) broadcasts here; signature-guarded. -->
        <receiver
            android:name=".KioskExitReceiver"
            android:exported="true"
            android:permission="com.aioapp.mdm.permission.KIOSK_EXIT">
            <intent-filter>
                <action android:name="com.aioapp.mdm.action.KIOSK_EXIT_PROMPT" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
